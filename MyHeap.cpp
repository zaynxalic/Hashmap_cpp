#include "MyHeap.h"

void myHeap::max_heapify()
{
}

void myHeap::build_Heap()
{
}

/* this function uses Max_heap function to generate K minium value of Deadline*/
vector<int> myHeap::KminDate(int package_num, vector<pair<time_t, int>> packinfo)
{
	vector<int> i{ 1, 1, 1 };
	cout << "Getting " << package_num << " rows from Packinfo table to deliver using Max-Heap..." << endl;
	cout << endl;
	return i;
}
