#include "Destinfo.h"
#include <iostream>
using namespace std;

/* generating a 1d vector of */
vector <int> destinfo::packageToSentDestination(vector<int> ids) {
	vector<int>  i { 1, 1, 1 };
	cout << "Analysising which places to send the parcel" << endl;
	cout << endl;
	return i;
}

vector<vector<int>> destinfo::packageMatrix(vector<int> city){
	vector<vector<int> > i { { 1, 1, 1 },{ 2, 2, 2 } };
	cout << "Generating the Distance Matrix of parcel to sent..." << endl;
	cout << endl;
	return i;
}

void destinfo::setPackinfo(int package_num){
	cout << "Initialising Destinfo matrix ..." << endl;
	cout << endl;
}

